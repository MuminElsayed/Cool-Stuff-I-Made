using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SegmentClass : MonoBehaviour
{
    public Transform endPos; //Saves the endPosition of the segment, so the next one spawns in front of it. 
}
